# app/api/routes/advance_scraper/constants.py
"""Fixed scrape settings — callers only choose search term + location."""

DAILY_COMPANY_LIMIT = 50
DEFAULT_MAX_DISCOVERY_WORKERS = 4
DEFAULT_PER_ZIP_CAP = 20
# How many rows to keep in the live preview payload while a run is active.
LIVE_PREVIEW_ROWS = 200
